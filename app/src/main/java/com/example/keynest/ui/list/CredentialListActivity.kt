package com.example.keynest.ui.list

import androidx.appcompat.app.AppCompatActivity

/**
 * Launcher activity stub. The full implementation (list rendering, FAB,
 * Autofill enable redirection) is added in tasks T9.1 / T9.5.
 *
 * This stub exists so that the AndroidManifest declares a launchable Activity
 * from task T1.4 onward.
 */
class CredentialListActivity : AppCompatActivity()
