<?xml version="1.0" encoding="utf-8"?>
<!--
    Issue #90 / Parent #89:
    KeyNest がサポートする credential type を OS に伝える。
    本 Issue では public-key credential (PassKey) のみを宣言する。
    パスワード型 (TYPE_PASSWORD_CREDENTIAL) は既存 KeyNestAutofillService 経路で
    継続するため、credential-provider 経路への二重露出を避ける目的で
    本 xml には宣言しない (requirements 2.3)。

    discoverable / non-discoverable の制限属性は付与しない (requirements 2.4)。
-->
<credential-provider xmlns:android="http://schemas.android.com/apk/res/android">
    <capabilities>
        <!--
            CRITICAL: the attribute is bare `name`, NOT `android:name`. The
            framework's credential-provider XML parser ignores `android:name`
            and only reads `name` (no namespace) — declaring `android:name`
            causes the system to treat the service as having zero capabilities,
            which Logcat surfaces as
            "Service does not have the required capabilities".
            Verified against the official Android docs sample and the
            Google PasswordAndPasskeyService / 1Password / Microsoft
            Authenticator providers, all of which use the bare `name` form.
        -->
        <capability name="androidx.credentials.TYPE_PUBLIC_KEY_CREDENTIAL" />
    </capabilities>
</credential-provider>
